FROM ghcr.io/datadrivenconstruction/openconstructionerp:latest

USER root
RUN apt-get update && apt-get install -y --no-install-recommends \
    libmupdf-dev \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir pymupdf

USER oe
